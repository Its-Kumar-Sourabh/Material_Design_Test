import React from 'react';

export default class contacts extends components {

    render() {
        return (
            <div>
                <h1>Contacts ...</h1>
            </div>

        );
    }
}