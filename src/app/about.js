import React from 'react';

export default class about extends components {

    render() {
        return (
            <div>
                <h1>About ...</h1>
            </div>

        );
    }
}